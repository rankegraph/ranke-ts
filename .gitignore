dist/
node_modules/

# Foundational papers pulled fresh from ranke-graph by `make docs` —
# fetched, never committed.
docs/papers/


# Local Claude config
/.claude/

# sindri hub artifacts (agent worktrees + hub state) — not for the repo
.worktrees/
.todos/
